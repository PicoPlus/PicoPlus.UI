# Presentation Layer

Contains UI composition: pages, components, layouts, and presentation-specific state.

Rules:
- No direct API/storage access.
- Use `Application` use-cases/contracts through dependency injection.
- Components should remain parameter-driven and reusable.
