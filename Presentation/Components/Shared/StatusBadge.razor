@using NovinCRM.Domain.Enums
@using NovinCRM.Domain.Extensions

@* Status badge component for deal stages *@

<span class="status-badge status-@Stage.GetBadgeClass()" role="status" aria-label="وضعیت: @Stage.GetDisplayName()">
    @Stage.GetDisplayName()
</span>

@code {
    [Parameter, EditorRequired]
    public NovinCRM.Domain.Enums.DealStage Stage { get; set; }
}

