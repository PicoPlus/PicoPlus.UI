@using PicoPlus.Application.Dto.UserPanel

@* Statistics cards container *@

<div class="row g-3 mb-4">
    <div class="col-lg-3 col-md-6">
        <StatisticsCard 
            Label="????? ????"
            Value="@Statistics.TotalRevenue"
            Unit="?????"
            IconClass="bi-graph-up-arrow"
            GradientClass="bg-gradient-blue"
            AnimationDelay="0.1s" />
    </div>

    <div class="col-lg-3 col-md-6">
        <StatisticsCard 
            Label="??????? ???"
            Value="@Statistics.OpenDeals"
            Unit="???"
            IconClass="bi-hourglass-split"
            GradientClass="bg-gradient-pink"
            AnimationDelay="0.2s" />
    </div>

    <div class="col-lg-3 col-md-6">
        <StatisticsCard 
            Label="??????? ????? ???"
            Value="@Statistics.ClosedDeals"
            Unit="???"
            IconClass="bi-check-circle"
            GradientClass="bg-gradient-green"
            AnimationDelay="0.3s" />
    </div>

    <div class="col-lg-3 col-md-6">
        <StatisticsCard 
            Label="?????? ??? ???"
            Value="@WalletBalance"
            Unit="?????"
            IconClass="bi-wallet2"
            GradientClass="bg-gradient-purple"
            AnimationDelay="0.4s" />
    </div>
</div>

@code {
    /// <summary>
    /// User statistics data
    /// </summary>
    [Parameter, EditorRequired]
    public UserStatistics Statistics { get; set; } = new();

    /// <summary>
    /// Wallet balance from contact info
    /// </summary>
    [Parameter]
    public decimal WalletBalance { get; set; }
}
